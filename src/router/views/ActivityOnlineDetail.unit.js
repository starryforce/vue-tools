import ActivityOnlineDetail from './ActivityOnlineDetail'

describe('@views/ActivityOnlineDetail', () => {
  it('is a valid view', () => {
    expect(ActivityOnlineDetail).toBeAViewComponent()
  })
})
