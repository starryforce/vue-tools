import ActivityOfflineDetail from './ActivityOfflineDetail'

describe('@views/ActivityOfflineDetail', () => {
  it('is a valid view', () => {
    expect(ActivityOfflineDetail).toBeAViewComponent()
  })
})
